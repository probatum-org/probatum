# Negative scenario: the watched log file never exists — that is couldn't-run
# (exit 2), NOT a failure. Used by the root probatum.yaml (expects exit 2).
- log: /nonexistent/never-written.log
  absent: ["ERROR"]
